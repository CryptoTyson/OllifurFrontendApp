const uiState = {
  header:
    'droplist' /* available: mixed, droplist, mega, navscroll, hamburger, basic, search */,
  footer: 'basic' /* available: basic, blog, contact, sitemap */,
  corner: 'nav' /* available: chat, nav */,
};

export default uiState;
